# Live mode and networking
LIVE_MODE=false
TIMEOUT_MS=10000
RETRIES=2
THROTTLE_RPS=1
USER_AGENT=Mozilla/5.0 (compatible; HPEPartSurferBot/1.0)

# Optional HTTP/HTTPS proxy (one URL, applied to both protocols if set)
HPE_PROXY_URL=

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log
